# Backend API Configuration
# URL where the frontend can reach the backend API
# - For local development: http://localhost:8080
# - For Docker Compose: http://hockey-backend:8080 (container name)
# - For production: https://api.yourdomain.com
HOCKEY_BACKEND_URL=http://localhost:8080

# NextAuth Configuration
# Secret key for NextAuth.js session encryption
# Generate with: openssl rand -hex 32
# MUST be at least 32 characters long
NEXTAUTH_SECRET=your-very-secure-nextauth-secret-key-should-be-long-and-random

# Full URL where the application is hosted
# Used by NextAuth for callbacks and redirects
# - For local development: http://localhost:3000
# - For production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Trust host header (useful for proxies and Docker)
# Set to true in production behind a reverse proxy
# AUTH_TRUST_HOST=true

# Optional: Public API URL for client-side requests
# If not set, client uses same domain as frontend
# NEXT_PUBLIC_API_URL=http://localhost:8080

# MinIO Configuration for Frontend (if needed)
VITE_MINIO_ENDPOINT=http://localhost:9000
VITE_MINIO_BUCKET=hockey-uploads

# Production Configuration Example:
# HOCKEY_BACKEND_URL=http://backend:8080
# NEXTAUTH_URL=https://yourdomain.com
# NEXTAUTH_SECRET=generate-a-very-long-random-string-at-least-32-characters
# AUTH_TRUST_HOST=true
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# VITE_MINIO_ENDPOINT=https://minio.yourdomain.com
# VITE_MINIO_BUCKET=hockey-uploads-prod
