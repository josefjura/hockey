# Hockey Management Application Environment Variables

# Backend Configuration
HOCKEY_HMAC_KEY=your-secret-hmac-key-change-this-in-production-min-32-chars
DATABASE_URL=sqlite:///app/data/hockey.db

# CORS Configuration (Backend)
CORS_ORIGINS=http://localhost:3000,http://localhost:4000
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_HEADERS=*

# Frontend Configuration  
NEXTAUTH_SECRET=your-secret-nextauth-key-change-this-in-production-min-32-chars
NEXTAUTH_URL=http://localhost:4000
AUTH_TRUST_HOST=true

# Production Configuration
HOCKEY_DOMAIN=hockey.joseforge.cz
HOCKEY_GITHUB_REPOSITORY=josefjura/hockey

# Development URLs (docker-compose.yaml)
# For internal Docker communication (inside containers):
# HOCKEY_BACKEND_URL=http://hockey-backend:8080
# For external access (browser to backend):
# HOCKEY_BACKEND_URL=http://localhost:9080

# Production URLs (docker-compose.prod.yaml with Traefik)
HOCKEY_NEXTAUTH_URL_INTERNAL=http://hockey_web:3000
HOCKEY_NEXT_PUBLIC_API_URL=https://api.hockey.dev
HOCKEY_NEXT_PUBLIC_ENV=production

# Production build notes:
# - Next.js bakes environment variables at BUILD TIME
# - Ensure HOCKEY_BACKEND_URL=http://hockey-backend:8080 during frontend build
# - Backend communicates internally on port 8080, Traefik handles external routing

# Database storage:
# - Development: ./data/hockey.db (host directory mapping)
# - Production: /home/beardo/data/hockey.db (production server path)
# - Backup location: Host filesystem directory
# - No Docker volumes used - direct host filesystem access

# CORS Configuration Notes:
# - CORS_ORIGINS: Comma-separated list of allowed origins or "*" for all
#   Development: "http://localhost:3000,http://localhost:4000"
#   Production: "https://yourdomain.com" (specific domains recommended)
# - CORS_METHODS: Comma-separated list of allowed HTTP methods or "*" for all
# - CORS_HEADERS: Comma-separated list of allowed headers or "*" for all
#   Production: Use specific headers like "Content-Type,Authorization" for security

# Optional: Custom ports for development
# HOCKEY_BACKEND_PORT=8080
# HOCKEY_FRONTEND_PORT=3000