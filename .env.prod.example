# Hockey Management System - Production Environment
#
# SECURITY NOTICE: This is an EXAMPLE file. DO NOT commit actual secrets!
# Copy this file to .env.prod and fill in with actual production values.

# ============================================================================
# CRITICAL: Must be changed for production!
# ============================================================================

# Database Configuration
# For production with persistent storage, use an absolute path
DATABASE_URL=sqlite:/app/data/hockey.db

# Session Secret (REQUIRED - Generate with: openssl rand -hex 32)
# Must be at least 64 characters for production
SESSION_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# ============================================================================
# Application Settings
# ============================================================================

# Environment
ENVIRONMENT=production

# Server Port (default: 8080)
PORT=8080

# Logging level
RUST_LOG=info,hockey=debug

# ============================================================================
# Security Best Practices
# ============================================================================
#
# 1. NEVER commit .env.prod to version control
# 2. Generate strong random SESSION_SECRET (min 64 chars)
# 3. Use absolute database path in production
# 4. Rotate secrets periodically (every 90 days recommended)
# 5. Restrict file permissions: chmod 600 .env.prod
#
# ============================================================================
# Docker Deployment
# ============================================================================
#
# Docker Compose automatically loads .env.prod from the same directory
# Environment variables can also be passed via docker-compose.prod.yaml
#
# ============================================================================
